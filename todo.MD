# Stuff to get done when i'm not procrastinating or conditions warrant me being able to test the thing.
* Multiplayer, this has already been started with LightNetLib a few years back but the project whent through so many changes in that time I never finished it. Probably needs its own custom server.
Ideally should be as adaptable and moddable as the rest of the project.
* More script work for collisions, destructable environments and the like. I've already got a start on this as shown by the single shot weapon thing, I just haven't decided fully how to make it work. With the newer modular way i'm using to construct newer areas (as shown in the red warp) this should be much easier to do.
* More scripts for more weapon types. Stuff like pulse rifles and the like.
* making sure mac OS support and android actually work properly. I don't have any immediet plans to get a recent enough mac as they are rather expensive, but android support will hopefully be workable once I switch phones.
* A few tiny tweeks and optimizations to get materials and textures unloaded from memory when they are no longer needed. This is a task that'd probably take at most 5 minutes, I just haven't decided on the implementation yet.
* Look at upgrading the projects loading, possibly from a moo like database instead of having to have the areas stored as files and folders on the disk. In theory with the new modular system this is perfectly doable complete with an objects system that attaches to specific scripts, but it'll need some serious planning.
* Since i've decided to go with the moo like database and essentially turn the hole project into the video game version of a mud/moo (but still retaining offline and all existing features), give serious consideration to potentially layter on down the road seeing if the ship class can't be scrapt and replaced with 100 percent lua scripting. In theory this'll make the hole thing much more portible and a bit more flexible, since it'll allow to design video games with the same scale of a mud but that aren't space related. In theory this is actually quite easily doable, but i'd like to get the database developed first.
